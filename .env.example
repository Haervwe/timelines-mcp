# Timelines MCP - Environment Configuration

# === Storage Backend ===
# Options: sqlite, postgres
STORAGE_ADAPTER=sqlite

# === SQLite Configuration (for development/testing) ===
SQLITE_PATH=./data/timelines.db

# === PostgreSQL Configuration (for production) ===
# Uncomment and configure when ready for production:
# STORAGE_ADAPTER=postgres
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=timelines
# POSTGRES_PASSWORD=your_secure_password
# POSTGRES_DATABASE=timelines

# === Vector Backend ===
# Options: chroma, qdrant
VECTOR_ADAPTER=chroma

# === Chroma Configuration (for development/testing) ===
CHROMA_PERSIST_DIR=./data/chroma_db


# === Qdrant Configuration (for production) ===
# Uncomment and configure when ready for production:
# VECTOR_ADAPTER=qdrant
# QDRANT_HOST=localhost
# QDRANT_PORT=6333
# QDRANT_API_KEY=your_api_key


# === General Settings ===
# FASTMCP_TRANSPORT=http
# FASTMCP_HOST=127.0.0.1
# FASTMCP_PORT=8000
# FASTMCP_LOG_LEVEL=info
# FASTMCP_CORS_ORIGINS=*